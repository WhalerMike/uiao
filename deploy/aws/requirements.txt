# Deploy-time dependencies for the UIAO AWS SaaS CDK app (ADR-117).
# These are NOT runtime dependencies of uiao.saas — they are only needed to
# synthesize/deploy the infrastructure. The application's AWS runtime deps are
# the `[aws]` extra in pyproject.toml (boto3, sqlalchemy, asyncpg).
aws-cdk-lib>=2.140.0,<3.0.0
constructs>=10.0.0,<11.0.0
