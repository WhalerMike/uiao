# deploy/windows-server/requirements-windows.txt
# Python dependencies for UIAO AD Survey API on Windows Server 2026
# Install after: pip install -e impl/
# Then: pip install -r deploy/windows-server/requirements-windows.txt

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27          # for async Graph API calls and health check

# AD query
ldap3>=2.9           # LDAP3 with GSSAPI/Kerberos support on Windows

# Entra ID / MSAL
msal>=1.28
cryptography>=42     # for certificate credential loading

# Data validation
pydantic>=2.7

# UIAO substrate (already in impl/ but explicit for deploy clarity)
pyyaml>=6.0
