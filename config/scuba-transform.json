{
  "_comment": "Plane 1 SCuBA->IR transform config. All keys are optional.",

  "tenant_boundary_id": "boundary:tenant:m365:default",

  "drop_statuses": [],

  "_doc": {
    "tenant_boundary_id": "Boundary ID injected into Policy.scope when mapping KSIs to IR. Override per-tenant.",
    "drop_statuses": "List of SCuBA status values to exclude before transform (e.g. ['INFO'] to skip informational items)."
  }
}
