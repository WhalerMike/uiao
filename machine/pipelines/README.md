# machine/pipelines/

CI/CD definitions and GitHub Actions workflow configurations.

## Rules

- Only machine-parseable formats (`.yaml`, `.yml`).
- No Markdown or prose.
- Pipeline changes should be tested in a branch before merging to main.
