{
  "name": "uiao-servicenow-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Executes the ServiceNow kit Script Includes outside an instance, and lints them.",
  "scripts": {
    "test": "node run.js",
    "lint": "cd ../.. && ./scripts/servicenow-harness/node_modules/.bin/eslint ."
  },
  "devDependencies": {
    "eslint": "^9.13.0"
  }
}
