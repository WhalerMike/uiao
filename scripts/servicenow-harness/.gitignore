# node_modules is a build artifact; package-lock.json is NOT — it pins the
# exact dependency tree CI installs (`npm ci`), which is the point of having
# a lockfile in a repo that gates security-critical code.
node_modules/
harness-results.json
