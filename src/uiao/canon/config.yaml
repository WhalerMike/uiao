# UIAO canon — top-level config (placeholder stub).
#
# This file exists so that `uiao validate` and similar CLI commands do not
# immediately error on a missing canon. It is replaced during migration from
# the legacy `uiao-core/canon/config.yaml`.
---
project: uiao
version: 0.2.0
status: placeholder

frameworks:
  fedramp_moderate: {}
  zta: {}
  tic3: {}

controls: {}
ksi: {}
rules: {}
oscal: {}
mappings: {}
