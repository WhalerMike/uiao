# Most recent enforcement blocks (action=block) — top 50 by dispatch
# timestamp descending. Used by SOC review of substrate enforcement
# activity in the last operational window.
source: enforcement
where:
  action: block
order_by: dispatched_at
order: desc
limit: 50
