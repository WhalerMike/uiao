# Every finding at High severity (or P1/P2 in the drift vocabulary).
# Used for daily standups and the substrate-status dashboard's
# "what's hot today" tile.
source: findings
where:
  severity:
    op: in
    value: [High, P1, P2, Critical]
order_by: severity
order: desc
limit: 100
