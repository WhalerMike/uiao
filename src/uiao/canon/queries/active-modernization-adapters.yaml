# All active phase-1 modernization adapters. Output is the canonical
# list of adapters every Phase-1 conformance gate scans against.
source: adapters
where:
  status: active
  phase: phase-1
order_by: id
order: asc
