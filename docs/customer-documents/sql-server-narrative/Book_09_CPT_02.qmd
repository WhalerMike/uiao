---
title: "The 2027 Milestone — What Done Looks Like"
doc-type: sql-server-narrative-chapter
audience: [Technical, Customer]
family: sql-server-narrative
book: "09"
series-order: 2
status: draft
lifecycle: active
tiers_adopted: []
lifecycle_review: 2027-01-01
---

# Chapter 2 — The 2027 Milestone: What Done Looks Like

*The evidence is not a binder you assemble — it is the live output of the monitoring you never turn off.*

::: {.callout-note}
## In this chapter
The six evidence artifact sets that close Transformation #7 — and why each is a live output of the continuous monitoring infrastructure rather than a point-in-time document — followed by the correct reading of the program's NTLM elimination strategy: not a fixed cutover date, but a posture maintained through continuous verification ahead of Microsoft's progressive NTLM defaults.
:::

The evidence artifacts required to close Transformation #7 in UIAO_135 are not a collection of documents produced at a single point in time — they are the live outputs of the continuous monitoring infrastructure described in Chapter 1. A closure claim is only as strong as the monitoring that keeps producing the evidence after the claim is made. This chapter names the artifacts, then places the program's NTLM elimination strategy in its proper operational context: continuous verification ahead of Microsoft's progressive NTLM defaults, not a fixed cutover date. (UIAO_135 §1.2, ADR-068 Phase C)

![The 2027-04-01 timeline showing migration complete before the network block, with the final months reserved for continuous-audit validation](images/book09cpt02-diagram-01-2027-block-timeline.png){#fig-book-09-cpt-02-diagram-01 fig-alt="A horizontal engineering timeline on a white background showing two sequential phases. The first phase is a wide navy band labeled migration window — Phase 1 create, Phase 2 validate, Phase 3 cutover. The second is a teal band labeled continuous-audit validation period, annotated CCM-BIR satisfying continuously. Six small teal artifact tags sit above the validation band labeled CCM-BIR records, Entra sign-in logs zero NTLM/Kerberos fallback, Defender for SQL zero NTLM, sa-disabled report, Conditional Access MFA report, exception registry. A navy caption beneath the timeline reads vendor defaults: progressive warnings, each release narrowing the window. Engineering blueprint style, white background, federal navy (#1F3A5F) for the migration band and labels, teal (#1A9E8F) for the validation band and satisfied artifact tags, monospace labels, no human figures, 16:9 landscape." width="100%"}

## The Evidence Artifacts as Live Outputs

The closure claim for Transformation #7 must be supported by six artifact sets, and each is a continuously regenerated output rather than a static deliverable. The first is the CCM-BIR record set: a record for every production SQL Server instance in the estate, each showing Entra-only authentication mode, zero active SQL Authentication logins, zero active Windows-backed logins, the Arc agent enrolled and healthy, and the SQL extension deployed. ADR-091 §5 defines the field set these records carry, and the continuous audit keeps them current rather than freezing them at the moment of the claim. (ADR-091 §5)

The second is the Entra ID sign-in log set, showing zero failed token requests attributable to NTLM or Kerberos fallback for SQL Server resource audience tokens over a defined validation period preceding the closure claim. The third is the Defender for SQL telemetry, showing zero NTLM authentication events over that same validation period — the behavioral confirmation that complements the structural CCM-BIR view. These two artifacts together demonstrate that the transformed posture holds not only in configuration but in observed connection behavior.

The fourth is the `sa` account status report, showing the `sa` account disabled on all instances. The fifth is the Conditional Access policy compliance report, showing MFA enforcement satisfied for one hundred percent of SQL Server resource sign-ins by human principals over the validation period. The sixth is the exception registry, showing every currently excepted instance with documented justification, a sunset date, and its own CCM-BIR tracking record. When all six artifact sets are simultaneously satisfying, and the monitoring infrastructure confirms they have been satisfying for the validation period, Transformation #7 is closed. That is what done looks like — not a checkbox, but a demonstrated, continuously verified, evidence-backed posture. (UIAO_135 §1.2, ADR-091 §5)

## Vendor Defaults: Progressive Warnings, Not a Hard Cutover

The program's NTLM elimination strategy is sometimes misread as a race to a single fixed date. It is not. Microsoft's progressive NTLM defaults — enhanced auditing in Windows Server 2025 / Windows 11 24H2, the `BlockNTLMv1SSO` enforcement default arriving via Windows Update in October 2026, and network NTLM off by default in the next major Windows Server release — raise the cost of delay with each release. The program's elimination posture is designed to stay ahead of that vendor default-disable trajectory, not to wait for a hard cutover.

Read as progressive vendor pressure rather than a single deadline, the schedule organizes differently. Migration activity — the three-phase parallel-run, the cutover, the `sa` disablement, the type-S elimination — should conclude with enough margin for the continuous audit to confirm the satisfying posture across a full validation period before Phase C is applied. The final months of the migration program are reserved for validation, not for last-minute migration, because a posture confirmed only at the edge of the window is a posture nobody has had time to prove.

This framing also gives the exception long-tail its proper weight. An instance that cannot reach Kerberos or Entra OAuth as dependencies are remediated must carry a documented exception with Certificate-Based Authentication as the authorized interim posture, and that exception's sunset date must be reconciled against the vendor default-disable trajectory rather than against any internal milestone. When the migration is complete, the audit has validated the posture continuously, and the six artifact sets are live and satisfying, the Phase C block arrives as a non-event — which is exactly what done is supposed to feel like. (ADR-068 Phase C and ADR-091 §5)

::: {.callout-tip title="Division of labor"}
**Microsoft provides:** the six evidence artifact categories — CCM-BIR records, Entra sign-in logs, Defender for SQL telemetry, `sa`-disabled report, Conditional Access MFA report, exception registry; the progressive NTLM default-disable that raises the cost of delay with each release.

**What only you can provide:** migration completion with sufficient margin for continuous-audit validation before Phase C is applied; the exception registry with Certificate-Based Authentication as the authorized interim posture; the documented sunset dates for every exception that must resolve ahead of the vendor default-disable trajectory.
:::

::: {.callout-note title="What Book 09 establishes — Chapter 2"}
The six evidence artifact sets that close Transformation #7 — CCM-BIR records, Entra sign-in logs showing zero NTLM/Kerberos fallback, Defender for SQL zero-NTLM telemetry, the `sa`-disabled report, the Conditional Access MFA report, and the exception registry — are live outputs of the continuous monitoring infrastructure, satisfying across a defined validation period rather than assembled once. The program's NTLM elimination strategy keeps pace with Microsoft's progressive default-disable trajectory: migration concludes with margin, the final period is reserved for continuous-audit validation, and Phase C is applied as a non-event.
:::
