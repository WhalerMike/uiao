---
title: "UIAO Azure SaaS"
subtitle: "Running UIAO as a multi-tenant governance SaaS on Azure Container Apps — architecture, deployment, tenant onboarding, and operations"
doc-type: architecture-explainer
audience: "Platform engineers · cloud operators · identity architects"
canon-source: "src/uiao/canon/adr/adr-096-azure-saas-architecture.md"
derived-from: "ADR-096 (Azure SaaS architecture); ADR-115 (SaaS production-readiness — quotas, rate limiting, audit, problem+json); ADR-116 (Azure deployment hardening — passwordless Postgres, IaC validation gate); ADR-033 (commercial endpoints also serve GCC-Moderate); ADR-084 (Phase 5 consumer architecture)"
aspirational: true
lifecycle: aspirational
tiers_adopted: [1]
lifecycle_review: 2026-12-19
format:
  html:
    toc: true
    toc-depth: 3
  docx:
    toc: true
---

# UIAO Azure SaaS {.unnumbered}

::: {.callout-note}
This page is the operator-facing articulation of the **UIAO Azure SaaS**
deployment — the multi-tenant governance plane that lets one operated
service govern many customer Entra tenants. The normative source is
[ADR-096](../../adr/adr-096-azure-saas-architecture.qmd); the implementation
lives in the `uiao.saas` package and `deploy/azure/`. The in-repo companion
to this page is
[`deploy/azure/README.md`](https://github.com/WhalerMike/uiao/blob/main/deploy/azure/README.md).
:::

::: {.callout-note}
## Where this fits — the cross-boundary plane
This multi-tenant plane is how OrgPath delivers the fifth differentiated
capability in [Book 18 — OrgPath and the IGA Market](../orgpath-narrative/Book_18.qmd):
a **single reconciled organizational substrate across many tenants and federal
cloud boundaries**, governed by one runtime. One operated service governs many
customer Entra tenants, acquiring per-tenant Graph/ARM tokens with the correct
boundary endpoints and audiences — the "one plane" claim Book 18 §6 argues no
single-boundary product can make. The per-deployment operator path is the
[OrgPath Implementation Guides](../operational-guides/orgpath-implementation/index.qmd);
the runtime it scales is the [OrgPath Runtime](./orgpath-runtime.qmd).
:::

::: {.callout-important}
**Status — shipped but dormant.** The code is merged and tested; the deploy
workflow (`.github/workflows/azure-saas-deploy.yml`) is **manual-dispatch
only** and nothing is deployed until an operator explicitly triggers it.
A concrete `AzureStampExecutor` now provisions the per-tenant resources
(Postgres schema, Blob container, Key Vault secret scope), but stamping stays
**dry-run by default**: it only executes when `UIAO_SAAS_STAMP_EXECUTION_ENABLED`
is set. Treat this page as the target operating model.
:::

::: {.callout-note}
## Recently hardened (ADR-115, ADR-116)
This plane now ships a **production-readiness layer** —
per-plan quotas + per-tenant rate limiting, an append-only control-plane audit
trail (`GET /control/v1/audit`), RFC 9457 `application/problem+json` errors,
and a real `/readyz` readiness probe ([ADR-115](../../adr/adr-115-saas-production-readiness.qmd))
— and **passwordless Postgres**: the managed identity is the database's Entra
administrator and the app authenticates with a short-lived token, so there is
no long-lived DB password ([ADR-116](../../adr/adr-116-azure-saas-deployment-hardening.qmd)).
Both are detailed in [§Production-readiness](#production-readiness--quotas-rate-limiting-audit-and-the-passwordless-data-tier) below.
:::

## 1. What this is

UIAO ships in two deployment shapes that share one codebase:

- **Single-tenant (Windows/IIS)** — the original surface in
  `deploy/windows-server/`. One agency runs UIAO inside its own boundary,
  with Kerberos inbound auth and a single Microsoft Graph application token.
  Unchanged by the SaaS work.
- **Multi-tenant SaaS (Azure Container Apps)** — the surface described here.
  One operated service governs **many** customer Entra tenants. Each customer
  grants admin consent to the UIAO multi-tenant app registration; UIAO then
  acquires **per-tenant** Microsoft Graph and Azure Resource Manager tokens
  to run governance passes on their behalf.

The SaaS surface is **additive** — it composes onto the existing `uiao.api`
data plane via `uiao.saas.attach_saas()` rather than forking it.

## 2. Why Azure Container Apps

The SaaS plane is the "behind-the-scenes" governance runtime. Container Apps
is the chosen compute target because:

- **Managed identity is the governance principal.** Each revision runs under
  a user-assigned managed identity — exactly the identity the existing
  `EntraTokenProvider`, `GraphTransport`, and `ArmTransport` expect for
  acquiring Graph / ARM tokens. No client secrets on disk.
- **Scale-to-zero + KEDA** suits bursty, scheduled governance passes.
- **Managed ingress, TLS, and revisions** give blue/green for free.
- It is lighter to operate than AKS and more elastic than App Service.

## 3. Architecture

![UIAO Azure SaaS architecture: customer Entra tenants send inbound JWTs (tid claim) to one Azure Container App running `uiao.saas.asgi:app`, which hosts the data plane (TenantResolutionMiddleware + uiao.api routes) and the control plane (/control/v1), uses a managed identity for per-tenant Graph/ARM tokens, and is backed by PostgreSQL, Key Vault, and Blob Storage.](images/saas-architecture.png){#fig-saas-architecture fig-alt="Engineering blueprint diagram on a white background. Three ice-blue navy-bordered boxes at top labeled Customer tenant A, B, C under the caption Entra ID customer tenants admin-consented. A teal arrow labeled inbound JWT tid claim points down into a large ice-blue box labeled Azure Container App uiao.saas.asgi:app, which contains two navy boxes — Data plane with TenantResolutionMiddleware plus uiao.api governance routes, and Control plane /control/v1 with onboard suspend resume deprovision — above a teal pill reading Managed identity to per-tenant Microsoft Graph / ARM tokens. Three teal arrows descend to three navy boxes labeled PostgreSQL tenant registry plus state, Key Vault per-tenant secrets, and Blob Storage evidence bundles. No human figures, 16:9 landscape." width="100%"}

The two planes share one process and one `/healthz` liveness endpoint:

- **Data plane.** Every non-public request must carry an Entra access token.
  `TenantResolutionMiddleware` verifies it, resolves the `tid` claim to a
  registered tenant, rejects non-onboarded or non-active tenants with `403`,
  and binds a `TenantContext` for the life of the request. Any governance
  code downstream reads `uiao.saas.require_current_tenant()` to learn whose
  data it is operating on — without threading the tenant through every call.
- **Control plane.** `POST /control/v1/tenants` and the lifecycle routes are
  the operator surface. They require a **publisher-tenant** token carrying
  the `UIAO.SaaS.Admin` app role.

## 4. Tenancy and data isolation

Each onboarded customer is a `Tenant` (`uiao.saas.tenant`): identified by its
Entra directory GUID, carrying a lifecycle status, a plan, and a
**`data_namespace`** derived deterministically from the GUID. That namespace
is the isolation handle — the Postgres row/schema scope, the Blob prefix, and
the Key Vault secret scope all hang off it.

The model is **per-request tenancy on a shared, elastically-scaled data
plane** (strong namespace isolation enforced in code) **plus a control-plane
stamp pattern** for customers whose compliance posture requires dedicated
resources. The lifecycle state machine is:

![Tenant lifecycle state machine: PENDING transitions to ACTIVE on onboard/consent; ACTIVE to SUSPENDED on suspend and back on resume; any state can be deprovisioned into the terminal DEPROVISIONED state. Only ACTIVE serves governance traffic.](images/saas-tenant-lifecycle.png){#fig-saas-lifecycle fig-alt="Engineering blueprint state diagram on a white background. An ice-blue PENDING box has a teal arrow labeled onboard / consent to a teal ACTIVE box annotated serves governance traffic, which has a teal arrow labeled suspend to an ice-blue SUSPENDED box, and a curved teal return arrow labeled resume. Red arrows from all three states converge downward, labeled deprovision from any state, into a navy DEPROVISIONED box annotated terminal. No human figures, 16:9 landscape." width="100%"}

Only `ACTIVE` tenants receive governance traffic; the middleware rejects all
others.

## 5. Prerequisites

Before the first deploy, an operator needs:

1. An Azure subscription and a target **resource group**.
2. A **multi-tenant app registration** (the UIAO app customers consent to),
   with the Microsoft Graph / ARM application permissions the governance
   passes require, and an Application ID URI (e.g. `api://uiao`).
3. An app role `UIAO.SaaS.Admin` defined on that registration, assigned to
   the operators who will drive the control plane.
4. A federated (OIDC) deployer identity for the GitHub Actions workflow, with
   `Contributor` on the resource group. See the secrets listed in
   `.github/workflows/azure-saas-deploy.yml`.

## 6. Configuration

The container reads `UIAO_SAAS_*` environment variables (see
`uiao.saas.settings.SaasSettings`). The Bicep wires these from module outputs
and secrets:

| Variable | Meaning | Source |
|---|---|---|
| `UIAO_SAAS_DATABASE_URL` | SQLAlchemy async DSN — **passwordless** (no embedded password) | Container Apps secret |
| `UIAO_SAAS_DATABASE_USE_ENTRA_AUTH` | `true` — authenticate to Postgres with an Entra token (ADR-116) | Bicep (passwordless default) |
| `UIAO_SAAS_DATABASE_ENTRA_USER` | Managed-identity name = the Postgres connection username | Identity module |
| `UIAO_SAAS_APP_CLIENT_ID` | UIAO multi-tenant app client ID | App registration |
| `UIAO_SAAS_PUBLISHER_TENANT_ID` | Home tenant that owns the control plane | Operator |
| `UIAO_SAAS_API_AUDIENCE` | Audience inbound tokens must carry | App registration |
| `UIAO_SAAS_CLOUD` | `commercial` (also GCC-Moderate), `gcc-high`, `dod` | Operator |
| `UIAO_SAAS_KEY_VAULT_URI` | Per-tenant secret store | Key Vault module |
| `UIAO_SAAS_STORAGE_ACCOUNT_URL` | Evidence-bundle storage | Storage module |
| `UIAO_SAAS_RATE_LIMITING_ENABLED` | Per-tenant request-rate limiting (default on, ADR-115) | Operator |
| `UIAO_SAAS_AUDIT_ENABLED` | Control-plane audit trail + `GET /control/v1/audit` (default on) | Operator |
| `UIAO_SAAS_STAMP_EXECUTION_ENABLED` | Execute (not just plan) per-tenant resource stamps | Operator (default off) |
| `AZURE_CLIENT_ID` | Managed-identity client ID for Graph/ARM + Postgres tokens | Identity module |

Without `UIAO_SAAS_DATABASE_URL` the runtime falls back to an **in-memory**
tenant registry — useful for local dev, never durable.

## 7. Deploy

[![Deploy to Azure](https://aka.ms/deploytoazurebutton)](https://portal.azure.com/#create/Microsoft.Template/uri/https%3A%2F%2Fraw.githubusercontent.com%2FWhalerMike%2Fuiao%2Fmain%2Fdeploy%2Fazure%2Fbicep%2Fmain.bicep)

The CI workflow `.github/workflows/azure-saas-deploy.yml` is **manual-
dispatch only**. To deploy by hand with the Azure CLI:

```bash
# 1. Stamp infrastructure (ACR, Postgres, Key Vault, Storage, Container App…).
#    Postgres is passwordless (Entra-only, ADR-116) — there is no DB password
#    to supply.
az deployment group create -g <rg> \
  -f deploy/azure/bicep/main.bicep \
  -p deploy/azure/bicep/main.bicepparam

# 2. Build and push the image
az acr build --registry <acr> --image uiao-saas:latest \
  --file deploy/azure/Dockerfile .

# 3. Re-deploy pointing at the pushed image
az deployment group create -g <rg> \
  -f deploy/azure/bicep/main.bicep \
  -p deploy/azure/bicep/main.bicepparam \
  -p containerImage="<acr>.azurecr.io/uiao-saas:latest"
```

The Bicep modules under `deploy/azure/bicep/modules/` provision Log Analytics
+ App Insights, the managed identity, ACR (with `AcrPull`), Key Vault (RBAC,
with `Key Vault Secrets User`), PostgreSQL Flexible Server (Entra-admin bound to
the managed identity, password auth disabled), a Storage account (with `Storage
Blob Data Contributor`), the Container Apps environment, and the SaaS Container
App itself. The whole template graph is compile-validated on every
`deploy/azure/**` change by `.github/workflows/bicep-validate.yml` —
credential-free, before any deploy.

## 8. Onboarding a tenant

```bash
# Operator (publisher-tenant token with the UIAO.SaaS.Admin role):
curl -X POST https://<app-fqdn>/control/v1/tenants \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"tenant_id": "<customer-tenant-guid>", "display_name": "Acme", "plan": "standard"}'
```

The response includes an **admin-consent URL**. The customer's Global
Administrator visits it once to grant the UIAO app the Graph / ARM
permissions. The control plane records the tenant (`pending → active`) and
returns the per-tenant stamp steps. With `UIAO_SAAS_STAMP_EXECUTION_ENABLED`
set, the `AzureStampExecutor` actually provisions those resources — it creates
the tenant's Postgres schema, its Blob container, and a Key Vault secret-scope
marker, each named after the tenant's `data_namespace`; without the flag the
same steps are planned but not executed. From that point, requests bearing that
tenant's tokens are resolved automatically by the data plane.

## 9. Lifecycle operations

| Action | Call |
|---|---|
| List tenants | `GET /control/v1/tenants` |
| Inspect one | `GET /control/v1/tenants/{id}` |
| Suspend (blocks data plane) | `POST /control/v1/tenants/{id}/suspend` |
| Resume | `POST /control/v1/tenants/{id}/resume` |
| Deprovision (terminal) | `DELETE /control/v1/tenants/{id}` |

Suspension is immediate: the next data-plane request for a suspended tenant
returns `403 tenant_not_active`. Deprovisioning plans the un-stamp (archive
schema, retain evidence per policy, revoke secrets) and moves the tenant to
the terminal `DEPROVISIONED` state.

## 10. Security model

- **Inbound.** Tokens are signature-verified against the issuing tenant's
  JWKS (RS256) in production. The dev-only `insecure_allow_unsigned` switch
  (claims still validated) defaults **off**.
- **Authorization.** The data plane rejects non-onboarded and non-active
  tenants. The control plane requires a publisher-tenant token with the
  `UIAO.SaaS.Admin` app role.
- **Secrets.** There is **no long-lived database password** — the Postgres
  connection authenticates with a short-lived Entra token minted by the managed
  identity (ADR-116). Per-tenant client secrets live in Key Vault, never in the
  image or in git. Outbound Graph / ARM access uses the managed identity, not
  stored credentials.
- **Error surface.** Both planes emit RFC 9457 `application/problem+json` with
  a stable machine `error` code (ADR-115), so the standardized envelope never
  leaks token contents or internal detail.

## Production-readiness — quotas, rate limiting, audit, and the passwordless data tier

The plane ships a cloud-agnostic production-readiness layer
([ADR-115](../../adr/adr-115-saas-production-readiness.qmd)) plus passwordless
deployment hardening ([ADR-116](../../adr/adr-116-azure-saas-deployment-hardening.qmd)).

**Per-plan quotas + rate limiting.** Each `TenantPlan` (trial / standard /
enterprise / gov) maps to a `PlanQuota` with a sustained request rate + burst.
A per-tenant fixed-window limiter enforces it; over-budget requests get `429`
with `Retry-After` and `RateLimit-*` headers, and admitted requests carry the
same `RateLimit-*` headers so clients can self-throttle. The limiter is
best-effort per-replica by default; for a **globally-exact** limit across
replicas, set `UIAO_SAAS_RATE_LIMIT_REDIS_URL` to use the shared Redis-backed
limiter (ADR-118, the optional `[redis]` extra). Toggle with `UIAO_SAAS_RATE_LIMITING_ENABLED`.

**Control-plane audit trail.** Every lifecycle action (onboard / suspend /
resume / deprovision — success *and* rejection) is recorded as an immutable
`AuditEvent` with the publisher-admin actor, and exposed to admins at
`GET /control/v1/audit`. The default sink is in-memory; a durable sink is a
drop-in behind the `AuditSink` protocol. Toggle with `UIAO_SAAS_AUDIT_ENABLED`.

**Liveness vs. readiness.** `/healthz` is the static liveness probe;
**`/readyz`** confirms the tenant registry is actually reachable
(`SELECT 1`) and returns `503 degraded` otherwise, so a revision that cannot
reach Postgres is withheld from the ingress pool. The Container App readiness
probe targets `/readyz`.

**Passwordless Postgres.** The managed identity is bound as the PostgreSQL
Flexible Server's **Entra administrator** and password auth is disabled. The
app presents a short-lived OSSRDBMS token as the connection password
(`uiao.saas.pg_auth`) — no DB password reaches CI, the deployment, or the
Container App's secret store. The token audience is sovereign-cloud aware.

| Capability | Surface | Default |
|---|---|---|
| Per-plan quotas / rate limiting | `429` + `RateLimit-*` headers | on |
| Audit trail | `GET /control/v1/audit` (admin) | on |
| Problem+json errors | `application/problem+json` both planes | always |
| Readiness probe | `GET /readyz` | always |
| Passwordless Postgres | Entra token as DB password | on (Bicep) |

## 11. Local development

```bash
pip install -e ".[saas]"
export UIAO_SAAS_INSECURE_ALLOW_UNSIGNED_TOKENS=true   # dev ONLY
export UIAO_SAAS_API_AUDIENCE=api://uiao
export UIAO_SAAS_PUBLISHER_TENANT_ID=<your-tenant-guid>
uvicorn uiao.saas.asgi:app --reload
```

`tests/test_saas_app.py` exercises the full onboarding → lifecycle flow
against the in-memory registry, so it is the fastest way to see the control
plane and tenant resolution behave end to end.

## 12. Sovereign clouds

`UIAO_SAAS_CLOUD` selects `commercial` (which also serves GCC-Moderate per
[ADR-033](../../adr/adr-033-gcc-boundary-drift-class.qmd)), `gcc-high`, or
`dod`. It drives the Entra login authority, the issuer template inbound
tokens are validated against, and the Graph / ARM audiences the managed
identity requests — consistent with the adapter cloud-resolution convention.
A sovereign-cloud SaaS *offering* is a future decision and a listed ADR-096
review trigger; no sovereign SaaS is stood up today.

## 13. Verticals: SQL Server transformation (same plane, adjacent execution)

A governed vertical does **not** mean its every operation runs inside the
multi-tenant container. The SQL Server Identity Transformation
([ADR-097](../../adr/adr-097-sql-server-saas-placement.qmd)) is the worked
example of the placement rule: its **governance** is the *same* SaaS, its
**host-level execution** is *adjacent*, in the customer's boundary.

![SQL Server transformation placement: the UIAO Azure SaaS control & evidence plane (tenant onboarding, SQL-logins-to-Entra identity mapping, evidence fabric, ARM/managed-identity orchestration) on the left; the customer boundary on the right (Arc-connected host → execution runner → SQL Server engine). The SaaS orchestrates the runner and ingests signed evidence; it never connects directly to the SQL engine.](images/sql-saas-placement.png){#fig-sql-saas-placement fig-alt="Engineering blueprint diagram. Left: an ice-blue box titled UIAO Azure SaaS control and evidence plane containing four stacked boxes — Tenant onboarding plus data_namespace isolation; Identity mapping SQL logins to Entra principals; Evidence fabric auth audit KSI CCM-BIR; and a teal ARM plus managed-identity orchestration plan apply reconcile dry-run default. Right: a dashed-border box titled Customer boundary Arc-connected containing a navy Arc-connected host plus managed identity, a teal Execution runner UIAOSqlServerMigration PowerShell, and a navy SQL Server engine sysadmin credential stays in-boundary, connected top to bottom with login migration. Two teal arrows cross the gap: orchestrate from SaaS to runner, signed evidence from runner to SaaS. A red dashed line with an X along the bottom reads UIAO never opens a direct connection to the customer's SQL engine, ADR-092 lane discipline. No human figures, 16:9 landscape." width="100%"}

- **Same SaaS** — tenant onboarding + `data_namespace` isolation, the
  SQL-logins-→-Entra identity model, the evidence fabric (auth audit · KSI ·
  CCM-BIR), and ARM/managed-identity orchestration. SQL is the **server
  (Arc/ARM) case** of the endpoint plane, incorporated like Intune and Arc.
- **Adjacent execution** — the authentication audit, Arc enrollment, and login
  migration run on the **Arc-connected host** via an execution runner
  (`UIAOSqlServerMigration`, PowerShell). The control plane orchestrates
  (`plan`/`apply`/`reconcile`, dry-run default) and ingests **signed
  evidence**; sysadmin credentials and engine access never leave the customer
  boundary, and the SaaS never connects to the engine directly (ADR-092 lane
  discipline).

This is encoded in canon: `sql-server` is registered in the modernization
adapter registry with `runner-class: on-prem-self-hosted`.

## The cross-boundary "one plane" — Book 18 capability #5, in depth

The "Where this fits" callout at the top names this plane as the fifth
differentiated capability in
[Book 18 — OrgPath and the IGA Market](../orgpath-narrative/Book_18.qmd). This
section is the substance behind that pointer: *why* a multi-tenant SaaS, built
the way this page describes, is the concrete realization of the claim Book 18 §6
argues no single-boundary product can make — a **single reconciled
organizational substrate across many tenants and federal cloud boundaries**.

The claim rests on three properties this architecture already has, each
described operationally above:

- **One runtime, many tenants.** The same `uiao.api` governance code runs every
  tenant's drift pass; the SaaS surface is additive (`attach_saas()`), not a
  fork (§1). There is one engine, one Codebook model, and one drift taxonomy in
  force across the whole estate — so "reconciled by one runtime" is literal, not
  aspirational. A department renamed in the Codebook is reasoned about
  identically for every tenant.
- **Per-tenant tokens, boundary-correct.** The managed identity acquires
  **per-tenant** Microsoft Graph and ARM tokens (§1–§3), and `UIAO_SAAS_CLOUD`
  selects the Entra login authority, the inbound issuer template, and the Graph
  / ARM audiences for `commercial` (also GCC-Moderate), `gcc-high`, or `dod`
  (§12). This is exactly Book 18 §6's point: the substrate model never changes
  across boundaries, only the host and token audience do — and the SaaS selects
  them per boundary rather than assuming the commercial defaults.
- **Isolation that survives the shared plane.** Each tenant's
  `data_namespace` (§4) scopes its Postgres rows, Blob prefix, and Key Vault
  secrets; `TenantResolutionMiddleware` binds a `TenantContext` per request and
  rejects non-active tenants. One reconciled *plane* does not mean one
  commingled *dataset* — the boundaries between tenants (and between clouds)
  stay intact while the substrate is governed uniformly across them.

Set against the IGA market Book 18 surveys, this is the capability with the
sharpest edge. The convergent IGA platforms — and Microsoft's own primitives —
are overwhelmingly single-boundary: an estate that spans Commercial,
GCC-Moderate, GCC-High, and DoD ends up maintaining a separate copy of its
organizational structure in each, with nothing keeping them in agreement. This
plane is the mechanism that keeps them in agreement: one governed Codebook, one
typed drift engine, one runtime, reaching every tenant in every boundary
through the correct cloud-specific door.

> Reconciling across boundaries is not dissolving them. Each tenant keeps its
> own namespace and each cloud its own trust root and token audience; what is
> shared is the substrate model and the engine that governs it. That is the
> "one plane" Book 18 §6 defends — and the SQL Server vertical above (§13) is
> the first worked consumer of it.

## 14. Entra app registration walkthrough

This section expands §5 prerequisite 2 into actionable steps.

### 14.1 Create the multi-tenant app registration

In the [Entra admin center](https://entra.microsoft.com) under
**App registrations → New registration**:

| Field | Value |
|---|---|
| Name | `UIAO Governance SaaS` |
| Supported account types | **Accounts in any organizational directory (multi-tenant)** |
| Redirect URI | Leave blank (no user sign-in flow) |

After creating the registration, note the **Application (client) ID** —
this becomes `UIAO_SAAS_APP_CLIENT_ID`.

### 14.2 Add API permissions (application permissions)

Under **API permissions → Add a permission → Microsoft Graph → Application
permissions**, add:

| Permission | Why |
|---|---|
| `Directory.Read.All` | Read users, groups, OUs, devices |
| `Device.Read.All` | Device inventory for OrgPath encoding |
| `GroupMember.Read.All` | Group membership enumeration |

Under **Azure Service Management → Application permissions**, add:

| Permission | Why |
|---|---|
| `user_impersonation` | Arc resource tagging (if Arc writeback enabled) |

After adding: **Grant admin consent** for your publisher tenant. Customer
tenants grant consent through the admin-consent URL generated at onboarding.

### 14.3 Expose an API (for inbound token audience)

Under **Expose an API → Application ID URI**, set:

```
api://uiao
```

This value becomes `UIAO_SAAS_API_AUDIENCE`. Customers' tokens must carry
this audience.

### 14.4 Define the admin role (control plane)

Under **App roles → Create app role**:

| Field | Value |
|---|---|
| Display name | `UIAO SaaS Admin` |
| Allowed member types | Applications |
| Value | `UIAO.SaaS.Admin` |
| Description | `Allows driving the UIAO SaaS control plane` |

Assign this role to the identities (users or service principals) that will
drive `POST /control/v1/tenants` and lifecycle operations.

### 14.5 OIDC federation for GitHub Actions

Under **Certificates & secrets → Federated credentials → Add credential**:

| Field | Value |
|---|---|
| Federated credential scenario | GitHub Actions |
| Organization | `WhalerMike` |
| Repository | `uiao` |
| Entity type | Environment |
| GitHub environment name | `saas-prod` (or `saas-dev`, `saas-test`) |
| Name | `uiao-saas-prod` |

Repeat for each environment. Add the following GitHub secrets to the
matching environment:

| Secret | Value |
|---|---|
| `AZURE_CLIENT_ID` | App registration client ID |
| `AZURE_TENANT_ID` | Publisher tenant ID |
| `AZURE_SUBSCRIPTION_ID` | Target Azure subscription ID |
| `AZURE_RESOURCE_GROUP` | Target resource group name |

(No `PG_ADMIN_PASSWORD` — Postgres is passwordless under Entra auth, ADR-116.)

---

## 15. Post-deployment validation

After the deploy workflow completes or after a manual `az deployment group create`,
run these checks to confirm the deployment is healthy.

### 15.1 Container App health

```bash
# Get the Container App FQDN
FQDN=$(az containerapp show -g <rg> -n uiao-saas --query properties.configuration.ingress.fqdn -o tsv)
echo "App FQDN: $FQDN"

# Health check
curl -sf "https://$FQDN/healthz" && echo "PASS: /healthz" || echo "FAIL: /healthz"
```

### 15.2 Inbound token verification

Acquire a token for the UIAO audience from your publisher tenant and call
the data plane:

```bash
# Acquire token (publisher tenant, app-only)
TOKEN=$(az account get-access-token --resource "api://uiao" --query accessToken -o tsv)

# Data plane — should return 403 (no tenant registered yet) not 401/500
STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer $TOKEN" \
  "https://$FQDN/api/v1/orgtree/status")
echo "Data plane status: $STATUS"   # expect 403 (no tenant) or 200
```

### 15.3 Control plane — onboard a test tenant

```bash
# Acquire admin token (publisher tenant, UIAO.SaaS.Admin role)
ADMIN_TOKEN=$(az account get-access-token --resource "api://uiao" --query accessToken -o tsv)

# Onboard — dry-run stamp (UIAO_SAAS_STAMP_EXECUTION_ENABLED is off by default)
curl -X POST "https://$FQDN/control/v1/tenants" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"tenant_id": "<test-tenant-guid>", "display_name": "Validation Test", "plan": "standard"}' \
  | python3 -m json.tool

# Verify it appears in the registry
curl -s "https://$FQDN/control/v1/tenants" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | python3 -m json.tool

# Clean up
curl -X DELETE "https://$FQDN/control/v1/tenants/<test-tenant-guid>" \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### 15.4 Postgres connectivity

```bash
# From the Azure CLI — check Postgres is running and the DB exists
az postgres flexible-server db list -g <rg> -s <postgres-server-name> -o table
```

---

## 16. Monitoring and assess

### 16.1 App Insights KQL — key signals

Open **Application Insights → Logs** in the Azure portal and run these
queries against your UIAO App Insights workspace.

**Failed tenant onboards (last 24 h):**
```kusto
requests
| where timestamp > ago(24h)
| where url contains "/control/v1/tenants"
| where resultCode !in ("200", "201")
| project timestamp, resultCode, url, duration, operation_Id
| order by timestamp desc
```

**Data-plane rejections (403 tenant not active):**
```kusto
requests
| where timestamp > ago(1h)
| where resultCode == "403"
| extend customProps = tostring(customDimensions)
| where customProps contains "tenant_not_active"
| summarize count() by bin(timestamp, 5m)
| render timechart
```

**Slow governance passes (>10 s):**
```kusto
requests
| where timestamp > ago(24h)
| where url contains "/orgpath" or url contains "/api/v1"
| where duration > 10000
| project timestamp, url, duration, resultCode
| order by duration desc
```

**Authentication failures (MSAL / Graph outbound):**
```kusto
exceptions
| where timestamp > ago(1h)
| where outerMessage contains "401" or outerMessage contains "AADSTS"
| project timestamp, outerMessage, operation_Id
| order by timestamp desc
```

### 16.2 Tenant lifecycle audit

Run weekly or before a compliance review:

```bash
# List all tenants and their status
curl -s "https://$FQDN/control/v1/tenants" \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  | python3 -c "
import sys, json
tenants = json.load(sys.stdin)
for t in tenants:
    print(f\"{t['status']:12} {t['tenant_id']}  {t.get('display_name','')}\")
"
```

Flag any tenants in `PENDING` for more than 7 days (admin consent not
completed) and any `SUSPENDED` tenants for review.

### 16.3 Stamp execution status

If `UIAO_SAAS_STAMP_EXECUTION_ENABLED` is set, verify per-tenant resources
exist:

```bash
# Verify Postgres schema for a tenant
az postgres flexible-server execute -n <server> -u <admin-user> -p <pwd> \
  -d uiao --querytext "SELECT schema_name FROM information_schema.schemata WHERE schema_name LIKE 'tenant_%';"

# Verify Blob containers
az storage container list --account-name <storage-account> \
  --query "[?starts_with(name,'tenant-')].[name,properties.lastModified]" -o table
```

---

## 17. Disaster recovery

### 17.1 PostgreSQL restore

PostgreSQL Flexible Server is provisioned with geo-redundant backup by
default (7-day retention). To restore to a point in time:

```bash
# Restore to a specific point in time (creates a NEW server)
az postgres flexible-server geo-restore \
  --resource-group <rg> \
  --name uiao-saas-pg-restored \
  --source-server <original-server-resource-id> \
  --restore-time "2026-06-15T03:00:00Z" \
  --location eastus2

# Update the Container App to point at the restored server (passwordless DSN —
# the app still authenticates with the managed identity's Entra token). The
# restored server must also have the managed identity re-bound as its Entra
# administrator (az postgres flexible-server ad-admin create).
az containerapp secret set -g <rg> -n uiao-saas \
  --secrets "database-url=postgresql+asyncpg://<identity-name>@uiao-saas-pg-restored.postgres.database.azure.com:5432/uiao"

az containerapp update -g <rg> -n uiao-saas \
  --set-env-vars "UIAO_SAAS_DATABASE_URL=secretref:database-url"
```

After restoring, verify the tenant registry is intact:

```bash
curl -s "https://$FQDN/control/v1/tenants" -H "Authorization: Bearer $ADMIN_TOKEN" | python3 -m json.tool
```

### 17.2 Key Vault recovery

Key Vault has soft-delete (90 days) and purge protection enabled by the
Bicep module. To recover a deleted vault:

```bash
# List soft-deleted vaults
az keyvault list-deleted --query "[?contains(name,'uiao')]" -o table

# Recover
az keyvault recover --name <vault-name>
```

If the managed identity lost its `Key Vault Secrets User` role assignment
(e.g., due to resource group recreation), re-assign it:

```bash
IDENTITY_PRINCIPAL=$(az containerapp show -g <rg> -n uiao-saas \
  --query identity.userAssignedIdentities.*.principalId -o tsv)
KV_ID=$(az keyvault show -g <rg> -n <vault-name> --query id -o tsv)

az role assignment create \
  --role "Key Vault Secrets User" \
  --assignee $IDENTITY_PRINCIPAL \
  --scope $KV_ID
```

### 17.3 Evidence blob recovery

Blob Storage is provisioned with geo-redundant storage (GRS). If the
primary region is unavailable, initiate a failover:

```bash
# Check replication status
az storage account show -g <rg> -n <storage-account> \
  --query "[geoReplicationStats, primaryEndpoints.blob]" -o json

# Initiate failover (irreversible until Microsoft completes recovery)
az storage account failover -g <rg> -n <storage-account> --yes
```

::: {.callout-warning}
Storage account failover changes the account's FQDN in some configurations.
After failover, verify `UIAO_SAAS_STORAGE_ACCOUNT_URL` still resolves and
update the Container App secret if necessary.
:::

### 17.4 Full environment recreation

If the entire resource group is lost, redeploy from scratch using the CI
workflow (`azure-saas-deploy.yml`, `deploy_infra=true`). The Bicep deployment
is idempotent. Tenant data (Postgres + Blob) must be restored from backups
per §17.1 and §17.3 after infrastructure is up.

Recovery time objective reference:

| Component | Recovery path | Typical RTO |
|---|---|---|
| Container App | CI workflow redeploy | ~10 min |
| PostgreSQL | Point-in-time restore to new server | ~20–40 min |
| Key Vault | Soft-delete recovery | ~5 min |
| Blob Storage | GRS failover | ~1 h (Microsoft-managed) |
| End-to-end | All above in sequence | ~1–2 h |

---

## Provenance

- Decision record: [ADR-096](../../adr/adr-096-azure-saas-architecture.qmd).
- Implementation: `uiao.saas` (package) and `deploy/azure/` (container +
  Bicep + deploy workflow).
- In-repo operator companion:
  [`deploy/azure/README.md`](https://github.com/WhalerMike/uiao/blob/main/deploy/azure/README.md).
- Control-plane API reference:
  [CLI Reference §5](../../docs/cli-reference.qmd).
