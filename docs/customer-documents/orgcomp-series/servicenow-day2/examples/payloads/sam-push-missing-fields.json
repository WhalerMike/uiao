{
  "_comment": "400 example — access_item and approval_authority are missing. validateInboundPush() lists every missing field in one refusal, not just the first, so the SDIM field map can be fixed in one pass.",
  "sam_request_id": "IIQ-REQ-2026-0000600",
  "requested_for": "opm-hrit:emp:00294417"
}
