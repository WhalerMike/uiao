---
title: "Insider Risk Management by Organizational Position"
doc-type: orgpath-narrative-chapter
audience: [Technical, Customer]
classification: Controlled
boundary: GCC-Moderate
family: orgpath-narrative
book: "07b"
series-order: 2
lifecycle: adopted
tiers_adopted: [1]
lifecycle_review: 2026-11-22
---

# Chapter 2 — Insider Risk Management by Organizational Position

::: {.callout-note}
## In this chapter
Insider Risk Management policies score user activity against indicators — data exfiltration, departing-employee signals, policy-violation patterns. Which indicators apply, at what thresholds, and to whom, should follow organizational position rather than a hand-maintained user list. This chapter scopes IRM policies and their user membership through OrgPath, so a transfer or departure re-scopes the risk posture automatically, and so the people in scope are always exactly the people the segment's risk model intends.
:::

<!-- FIGURE INTENT (materialize via fig-alt once numbering final): white background, engineering blueprint style, 16:9. Left: an OrgTree branch /Root/Finance flagged InsiderRiskScope. Center: an amber join-key connector to a navy "IRM policy: Finance — heightened exfiltration indicators, departing-user triggers". Right: a reconciliation job box reading the OrgTree registry and emitting IRM scoped-policy membership via the Purview/Graph API, with a DRIFT-PROVENANCE marker where membership diverges from the registry. Monospace path/flag labels. No human figures. -->

## How Insider Risk Policies Are Scoped Today, and Why That Breaks

Describe the native model — IRM policies with users assigned directly or via groups, indicators and thresholds set per policy — and the failure mode: assignment drifts from organizational reality, departing/transferring users fall in or out of scope late, and the risk posture no longer matches the org.

## Differentiating Indicators and Thresholds by Organizational Tier

Develop the substance: high-sensitivity segments warrant heightened exfiltration indicators and tighter thresholds; general-population segments warrant a lighter posture. Tie the differentiation to the same `SegmentLabels`/scope-flag metadata convention Book 07 and Book 07a established on OrgTree nodes (e.g. an `InsiderRiskScope` flag).

## Scoping IRM Policy Membership Through OrgPath

The mechanism: a reconciliation job reads the OrgTree registry for nodes carrying the IRM scope flag, resolves the OrgPath membership, and maintains the policy's in-scope user set idempotently — the same pattern Book 07 used for Adaptive Scopes, applied to the IRM surface, which does not consume an Adaptive Policy Scope and must be driven explicitly.

## Departing-User and Mover Signals as a Lifecycle Event

Connect to the lifecycle engine: a join/mover/leaver event in the OrgTree pipeline is precisely the signal IRM most needs. Show how a departure recorded in the registry both triggers the heightened departing-user posture and, on completion, removes the user from scope — closing the window cleanly rather than leaving stale surveillance.

## Drift, Evidence, and the Privacy Boundary

Close on the guardrail: every membership change emits a `DRIFT-PROVENANCE` finding into the Evidence Bundle, and the reconciliation job is gated by the privacy review the next chapters formalize — IRM scoping decisions are surveillance decisions and must be auditable as such.
