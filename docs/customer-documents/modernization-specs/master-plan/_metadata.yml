# Shared frontmatter for the Modernization Master Plan section.
# Per-file frontmatter takes precedence; this file supplies defaults that
# every page in master-plan/ inherits unless overridden.

canon-source: "src/uiao/canon/modernization-registry.yaml"
aspirational: true
date-format: "MMMM D, YYYY"
