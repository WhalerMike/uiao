# DISABLED: MkDocs deploy — replaced by build-docs.yml (Quarto)
# This workflow was competing with build-docs.yml for the github-pages artifact slot,
# causing the MkDocs site to overwrite the Quarto site on every push.
# Quarto (build-docs.yml) is now the single authoritative Pages deployer.
# MkDocs / requirements.txt are retained for local preview only.

name: Deploy MkDocs to GitHub Pages (DISABLED)

on:
  workflow_dispatch:
      inputs:
            confirm:
                    description: 'Type ENABLE to re-enable this workflow'
                            required: true

                            jobs:
                              disabled:
                                  if: ${{ github.event.inputs.confirm == 'ENABLE' }}
                                      runs-on: ubuntu-latest
                                          steps:
                                                - run: echo "This workflow is disabled. Use build-docs.yml instead."
