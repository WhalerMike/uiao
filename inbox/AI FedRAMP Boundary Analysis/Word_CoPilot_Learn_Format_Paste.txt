Transform this entire document into strict Microsoft Learn–style formatting. Apply these rules consistently across the whole file:

1. Body text:
Convert all body text to Segoe UI, 11 pt, black. Use single spacing with 6 pt after each paragraph. Remove extra blank lines and normalize spacing.

2. Headings:
Convert Markdown headings (#, ##, ###, ####) into Word heading styles. Heading 1 should be Segoe UI 20 pt bold. Heading 2 should be Segoe UI 16 pt bold. Heading 3 should be Segoe UI 14 pt bold. Heading 4 should be Segoe UI 12 pt bold. Ensure correct spacing above and 6 pt below each heading.

3. Tables:
Convert any pipe tables or CSV/TSV blocks into real Word tables. Autofit tables to the page width. Bold the header row. Prevent rows from breaking across pages.

4. Lists:
Normalize all lists. Ensure Segoe UI 11 pt and consistent spacing.

5. Cleanup:
Remove HTML artifacts such as <p>, <br>, <div>, <span>, &nbsp;, and similar encoded characters. Remove double spaces. Remove triple paragraph breaks. Ensure clean, consistent formatting throughout.

6. Ensure Font is consistent across entire document to match Microsoft Learn style.

Apply all changes silently and return the fully formatted document.
