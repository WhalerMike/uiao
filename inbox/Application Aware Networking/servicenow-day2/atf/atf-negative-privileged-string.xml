<?xml version="1.0" encoding="UTF-8"?>
<!--
  ATF NEGATIVE - privileged as the STRING 'true' still triggers the expiry rule (AC-6)
  PRECONDITION: x_ssa_day2_ops.test_mode = 'true' - no live Graph connectivity.
  Scope: x_ssa_day2_ops.  STARTER SKELETON (see atf/README.md).

  ServiceNow catalog checkbox variables arrive as the STRING 'true', not boolean
  true. A gate testing truthiness naively would let a privileged grant through.
  EntraHelpdeskGate._truthy() exists for exactly this; this test pins it.
-->
<record_update table="sys_atf_test">
  <sys_atf_test action="INSERT_OR_UPDATE">
    <name>Day-2 negative: privileged as string true with no expiry still fails preflight</name>
    <sys_scope>x_ssa_day2_ops</sys_scope>
    <active>true</active>
    <description>A catalog-sourced privileged flag arrives as the string true. The gate must coerce it and still refuse a grant with no expiry - the string must not bypass AC-6.</description>
  </sys_atf_test>

  <sys_atf_step action="INSERT_OR_UPDATE">
    <order>100</order>
    <step_config_title>Server - Run Server Side Script</step_config_title>
    <description>privileged as the STRING true with no expiry; assert the gate still refuses.</description>
    <inputs><![CDATA[
(function(outputs, steps, stepResult) {
  var gate = new x_ssa_day2_ops.EntraHelpdeskGate();
  var verdict = gate.preflight({
    requester_id: 'day2-requester-sysid',
    approver_id: 'day2-approver-sysid',
    privileged: 'true'                    // STRING, as a catalog checkbox delivers it
  });
  if (verdict.ok) {
    stepResult.setFailed('string true bypassed the expiry rule - _truthy() coercion regressed (AC-6)');
    return;
  }
  stepResult.setSuccess();
})(outputs, steps, stepResult);
    ]]></inputs>
  </sys_atf_step>
</record_update>
