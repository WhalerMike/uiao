# UIAO-Core Development Dependencies
# Runtime deps are now in pyproject.toml [project.dependencies]
# Dev deps are in pyproject.toml [project.optional-dependencies.dev]
#
# This file is kept for backward compatibility with CI and legacy workflows.
# To install everything: pip install -e ".[dev]"
#
# Legacy runtime deps (reference only - authoritative source is pyproject.toml)
-e .

# Dev tools
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4.0
mypy>=1.10.0
pre-commit>=3.7.0
