# Publication Gap Report

Generated by `scripts/scan_publication_gaps.py` per ADR-072. Lists canon documents intended for the published site at <https://whalermike.github.io/uiao/> that have no corresponding `.qmd` entry in `docs/_quarto.yml` (Pass 1), and `.qmd` pages that exist on disk but are not registered in the sidebar (Pass 2).

## Unregistered pages — on disk but not in the sidebar (0)

_None. Every registration-required `.qmd` is wired into `docs/_quarto.yml`._

## Summary

```
Class              Total  Publishable  Published    Gap
---------------- ------- ------------ ---------- ------
uiao-spec             60           53         53      0
adr                  115          112        112      0
modernization         24           21         21      0
schema                36           35         35      0
other                118            1          1      0
---------------- ------- ------------ ---------- ------
TOTAL                353          222        222      0
```

## Methodology

**Pass 1 — source → sidebar (publication gap):**

1. Walk `src/uiao/canon/`, `src/uiao/modernization/`, and `src/uiao/schemas/`.
2. For each file, read frontmatter `publish_to_site` if present; otherwise apply ADR-072 path-pattern defaults.
3. For each file with `publish_to_site == true`, derive expected `.qmd` paths and check whether any of them appear in `docs/_quarto.yml`.
4. Report files with `publish_to_site == true` and no matching `.qmd` as gaps.

**Pass 2 — disk → sidebar (unregistered page):**

5. For each registration-required glob (`REGISTRATION_REQUIRED_GLOBS`), enumerate every matching `.qmd` on disk.
6. Report any match that is not referenced anywhere in `docs/_quarto.yml` as an unregistered page.

## How to close a gap

1. Decide the publication style (`narrative`, `include`, `reference`) and set it explicitly in the source document's frontmatter.
2. Author or generate the `.qmd` page at one of the expected paths (or any other path of your choice — then add it to `docs/_quarto.yml`).
3. Add the new `.qmd` to the sidebar in `docs/_quarto.yml`.
4. Re-run this scanner to confirm the gap is closed.

## How to suppress a false positive

If a document should NOT be published, add `publish_to_site: false` to its frontmatter with a brief justification in a comment. The scanner will then skip it on subsequent runs.
